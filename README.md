# Advanced Features and Security
- Custom user model with `date_of_birth` and `profile_photo`.
- Permissions: `can_view`, `can_create`, `can_edit`, `can_delete` for Book model.
- Security: CSRF tokens, XSS protection, HTTPS redirects.
- HTTPS: Configured with SECURE_SSL_REDIRECT and HSTS.